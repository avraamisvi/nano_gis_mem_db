pub struct Context {

}

impl Context {
    
    pub add_value(&mut self) {

    }

    pub fn resolve_value(&self, value: ValueContainer) -> ValueContainer {
        //TODO resolve identifiers when it is possible
        value
    }
}